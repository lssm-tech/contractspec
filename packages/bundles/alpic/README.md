# @contractspec/bundle.alpic

Alpic MCP server helpers bundled for apps and services.

```ts
import { createAlpicMcpApp } from "@contractspec/bundle.alpic";

const app = createAlpicMcpApp();
app.listen(8080);
```
