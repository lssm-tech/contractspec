# OpenCode CLI Example

This example demonstrates running ContractSpec with the OpenCode agent mode.

## Requirements

- A running OpenCode server (default: http://127.0.0.1:4096)
- `@opencode-ai/sdk` available in the workspace

## Usage

```bash
bun run build
bun run validate
```
