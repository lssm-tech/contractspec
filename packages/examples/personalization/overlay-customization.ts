import { runOverlayCustomizationExample } from './src/overlay-customization';

void runOverlayCustomizationExample().catch((error) => {
  // eslint-disable-next-line no-console
  console.error(error);
  process.exitCode = 1;
});






















