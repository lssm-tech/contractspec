{
  "name": "@contractspec/app.api-library",
  "version": "2.5.0",
  "private": true,
  "scripts": {
    "clean": "rm -rf dist",
    "lint": "bun run lint:fix",
    "lint:fix": "eslint src --fix",
    "lint:check": "eslint src",
    "dev": "bun run --watch src/index.ts",
    "build": "bun run build:types && bun run build:bundle",
    "build:bundle": "mkdir -p api && bun build --target node --sourcemap=none --packages bundle --outfile api/index.js src/index.ts",
    "build:types": "tsc -p ./tsconfig.json --noEmit",
    "start": "bun run ./api/index.js"
  },
  "dependencies": {
    "@contractspec/bundle.library": "workspace:*",
    "@contractspec/bundle.marketing": "workspace:*",
    "@contractspec/bundle.workspace": "workspace:*",
    "@contractspec/lib.contracts-spec": "workspace:*",
    "elysia": "^1.4.25",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@contractspec/tool.typescript": "workspace:*",
    "typescript": "catalog:"
  },
  "type": "module"
}
