'use client';

import { ExampleInlinePreviewSection } from '../ExampleInlinePreviewSection';

export function DataGridShowcaseDocsClient() {
	return (
		<ExampleInlinePreviewSection
			exampleKey="data-grid-showcase"
			title="Embedded canonical table demo"
			description="This page embeds the same example package used in the sandbox and marketing preview surfaces. Use it here when you want the docs context and the interactive table in one place."
		/>
	);
}
