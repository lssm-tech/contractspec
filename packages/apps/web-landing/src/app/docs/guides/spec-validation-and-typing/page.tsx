import type { Metadata } from 'next';
import { GuideSpecValidationTypingPage } from '@contractspec/bundle.library';

export const metadata: Metadata = {
  title: 'Spec Validation + Typing - ContractSpec Guides',
  description:
    'Define operation specs with SchemaModel validation and types without rewrites.',
  alternates: {
    canonical:
      'https://www.contractspec.io/docs/guides/spec-validation-and-typing',
  },
};

export default GuideSpecValidationTypingPage;
