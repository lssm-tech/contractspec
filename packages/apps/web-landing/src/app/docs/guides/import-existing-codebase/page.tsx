import type { Metadata } from 'next';
import { GuideImportExistingCodebasesPage } from '@contractspec/bundle.library';

export const metadata: Metadata = {
  title: 'Import existing codebase - ContractSpec Guides',
  description:
    'Add ContractSpec to an existing codebase by analysing the codebase.',
  alternates: {
    canonical:
      'https://www.contractspec.io/docs/guides/import-existing-codebase',
  },
};

export default GuideImportExistingCodebasesPage;
