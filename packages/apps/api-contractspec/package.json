{
  "name": "@lssm/app.api-contractspec",
  "version": "1.0.50",
  "scripts": {
    "clean": "rm -rf dist",
    "lint": "bun run lint:fix",
    "lint:fix": "eslint src --fix",
    "lint:check": "eslint src",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "export-schema": "bun run src/graphql/schema-export.ts",
    "ssl:gen": "openssl req -x509 -nodes -days 365 -newkey rsa:2048 -keyout certs/key.pem -out certs/cert.pem -config local/openssl.cnf"
  },
  "dependencies": {
    "@better-auth/expo": "1.4.5",
    "@elysiajs/swagger": "^1.3.1",
    "@lssm/lib.database-contractspec-studio": "workspace:*",
    "@lssm/lib.contracts": "workspace:*",
    "@pothos/core": "^4.9.1",
    "@pothos/plugin-relay": "^4.4.3",
    "better-auth": "1.4.5",
    "elysia": "^1.4.18",
    "graphql-scalars": "^1.24.2"
  },
  "devDependencies": {},
  "module": "src/index.js",
  "type": "module"
}
