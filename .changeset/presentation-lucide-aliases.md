---
"@contractspec/lib.presentation-runtime-core": patch
---

Add lucide package rewrites to the presentation runtime bundler helpers so Metro maps `lucide-react` to `lucide-react-native` and Next.js maps the native package back to `lucide-react`.
