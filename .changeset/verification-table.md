---
'@contractspec/lib.contracts': minor
'@contractspec/action.pr': minor
'@contractspec/action.drift': minor
---

Add contract-level verification table to impact reports. Defines query and data view contracts for per-contract drift status, surfaces covered, and last verified commit. Reports render the table when contracts data is present; backward compatible when absent.
