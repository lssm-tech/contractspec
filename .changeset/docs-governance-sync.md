---
"contractspec": patch
"@contractspec/lib.contracts-spec": patch
"@contractspec/bundle.library": patch
"@contractspec/app.web-landing": patch
---

Refresh the public docs and release-facing guidance around Connect, Builder, release capsules, and the current `@contractspec/lib.contracts-spec` surface.

- align root docs, package docs, website docs, and `/llms*` guidance with the recent changeset themes and current public workflows
- add a release-capsules guide and extend the Connect and Builder docs with adoption flows, Builder defaults, and operator status details
- replace the stale `contracts-spec` export inventory with a current `package.json`-derived summary and sync the generated web/package guides
