---
"@contractspec/app.web-landing": patch
"@contractspec/bundle.library": patch
---

Rewrite the OSS docs experience around a new docs manifest and primary navigation model. This refresh updates the web docs shell, sidebar, metadata wiring, reference entrypoints, and the main overview pages to present ContractSpec as an open spec system with Studio as an additive operating layer.
