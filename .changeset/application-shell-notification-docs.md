---
"@contractspec/bundle.library": patch
"@contractspec/app.web-landing": patch
---

Document AppShell in-app notification adoption on the public application shell docs page.

The guide now explains the shell boundary for notification contracts, runtime helpers, host-owned live state, and render-ready `ShellNotificationCenter` props. It also refreshes the copy-ready AI prompts for both greenfield shell implementation and existing-app refactors.
