---
"@contractspec/lib.design-system": minor
---

Add ThemeSpec-aware and TranslationSpec-aware design-system form controls, stack wrappers, and a theme runtime so product and FormSpec surfaces share the same cross-platform control boundary.
