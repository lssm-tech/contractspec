---
"@contractspec/lib.design-system": patch
---

Add ThemeSpec Tailwind aliases for shadcn option surfaces so FormSpec select and combobox popovers keep an opaque background when rendered from design-system theme CSS.
