---
"@contractspec/bundle.library": patch
"@contractspec/app.web-landing": patch
---

Document the ContractSpec translation runtime and optional i18next adapter on the public docs site.

The web docs now expose a dedicated `/docs/libraries/translation-runtime` page, link it from the library overview and primary docs sidebar, and keep `/llms.txt` plus the web-landing README aligned with the new guide and package-level `/llms/lib.translation-runtime` prompt surface.
