---
'agentpacks': patch
---

Improve AgentSkills compatibility by preserving full `SKILL.md` frontmatter in generated targets and Cursor plugin exports, validating required skill metadata in `agentpacks pack validate`, and normalizing imported skills with required `name`/`description` fields.
