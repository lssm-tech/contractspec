---
'@contractspec/lib.workflow-composer': minor
'@contractspec/lib.ai-agent': minor
'@contractspec/lib.contracts-spec': minor
'@contractspec/integration.runtime': minor
---

Harden workflow and agent loop execution with stricter validation, deterministic composition ordering, runtime timeout/cooldown controls, and pause/resume-capable workflow execution semantics.

Add adapter-first runtime interop surfaces and safer channel ingest policy gating so teams can adopt LangGraph/LangChain/useworkflow-style integrations without coupling core ContractSpec contracts to vendor runtimes.
