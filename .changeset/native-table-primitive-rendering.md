---
"@contractspec/lib.presentation-runtime-react": patch
"@contractspec/lib.presentation-runtime-react-native": patch
---

Keep shared table headers and cells as primitive render-model values so React Native table renderers can wrap string content in `Text`.
