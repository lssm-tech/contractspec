---
"@contractspec/bundle.library": patch
---

Document and link object-reference adoption and adaptive panel guidance in the public docs bundle.

The design-system library page now explains how to model rich object references,
where runtime handlers belong, how same-page versus new-page detail navigation is
configured, how adaptive sheet/drawer panels are selected, and includes an agent
prompt for replacing inert references across downstream applications. The docs
manifest now promotes the design-system page in the build sidebar with object
reference and AdaptivePanel aliases.
