schemaVersion: "1"
slug: finance-ops-demo-preview
summary: Expand the Finance Ops AI Workflows preview into a multi-screen template demo and refresh the generated LLMS package surface for it.
isBreaking: false
packages:
  - name: "@contractspec/example.finance-ops-ai-workflows"
    releaseType: minor
  - name: "@contractspec/app.web-landing"
    releaseType: patch
affectedRuntimes:
  - browser
  - nextjs
affectedFrameworks:
  - react
  - turbopack
audiences:
  - kind: maintainer
    summary: The finance-ops example now ships richer preview fixtures, split preview screens, and refreshed docs surface exports for the showcase route.
    affectedPackages:
      - "@contractspec/example.finance-ops-ai-workflows"
  - kind: integrator
    summary: Preview-consuming apps can surface a deterministic multi-screen finance-ops demo rather than a single static panel.
    affectedPackages:
      - "@contractspec/example.finance-ops-ai-workflows"
      - "@contractspec/app.web-landing"
  - kind: customer
    summary: The `/templates` Finance Ops AI Workflows preview now reads like a guided operator demo with scenario-specific screens and review flows.
deprecations: []
migrationInstructions: []
upgradeSteps:
  - id: use-finance-ops-preview-entrypoint
    title: Consume the richer finance-ops preview entrypoint
    summary: The example package keeps the same `./ui` surface, but hosts should expect multiple preview screens and supporting fixtures.
    level: assisted
    instructions:
      - Install `@contractspec/example.finance-ops-ai-workflows` in preview-consuming apps.
      - Regenerate any LLMS or docs package listings that enumerate example package surfaces.
validation:
  commands:
    - "bun run --cwd packages/examples/finance-ops-ai-workflows preflight"
    - "bun run --cwd packages/apps/web-landing build:types"
  evidence:
    - "Finance Ops AI Workflows preview is split into multiple deterministic scenario screens with dedicated fixture data."
    - "The generated LLMS package page for the example reflects the refreshed package surface."
knownGaps:
  - "No browser screenshot evidence is attached in this capsule; the preview remains deterministic but verification is currently package-local."
