---
"@contractspec/lib.design-system": patch
"@contractspec/lib.ui-kit-web": patch
---

Fix FormSpec password visibility toggles so caller-supplied password input props cannot keep the field masked after the user clicks the visibility control.
