---
"@contractspec/tool.bun": patch
---

Publish TypeScript declarations for the root config preset API so customer `tsdown.config.ts` and `bun.config.ts` files can import typed presets, `defineConfig`, and `BuildConfig` from `@contractspec/tool.bun`.
