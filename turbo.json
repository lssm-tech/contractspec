{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "clean": {
      "cache": false
    },
    "test": {},
    "publish:pkg": {
      "dependsOn": ["build:bundle", "^publish:pkg"],
      "cache": false,
      "env": ["NPM_CONFIG_TOKEN", "VSCE_PAT", "OVSX_PAT"]
    },
    "publish:pkg:canary": {
      "dependsOn": ["build:bundle", "^publish:pkg:canary"],
      "cache": false,
      "env": ["NPM_CONFIG_TOKEN", "VSCE_PAT", "OVSX_PAT"]
    },
    "build": {
      "cache": true,
      "dependsOn": ["^build"],
      "outputLogs": "new-only",
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*",
        "tsdown.config.js",
        "src/**",
        "!src/generated/**",
        "public/**"
      ],
      "outputs": [
        "dist/**",
        "generated/**",
        ".next/**",
        "!.next/cache/**",
        "src/generated/**",
        "src/handlers/**"
      ]
    },
    "build:bundle": {
      "cache": true,
      "dependsOn": ["^build:bundle"],
      "outputLogs": "new-only",
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*",
        "tsdown.config.js",
        "src/**",
        "!src/generated/**",
        "public/**"
      ],
      "outputs": [
        "dist/**",
        "generated/**",
        ".next/**",
        "!.next/cache/**",
        "src/generated/**",
        "src/handlers/**"
      ]
    },
    "build:types": {
      "cache": true,
      "outputLogs": "new-only",
      "dependsOn": ["^build:types"],
      "inputs": [
        "public/**",
        "src/**",
        "package.json",
        "tsconfig.json",
        "tsdown.config.js"
      ]
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "docs:generate": {
      "outputLogs": "new-only"
    },
    "lint": {
      "outputLogs": "new-only"
    },
    "lint:check": {
      "outputLogs": "new-only"
    }
  }
}
